import 'package:flutter/material.dart';

class LandingShowCard extends StatelessWidget {
  const LandingShowCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 200,
      width: 150,
      child: Placeholder(),
    );
  }
}
